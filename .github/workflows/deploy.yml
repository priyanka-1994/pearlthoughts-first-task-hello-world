name: Deploy to EC2

on:
  push:
    branches:
      - master  

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install Node.js and npm
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Install dependencies
        run: npm install

      - name: Build and Deploy
        run: |
          npm run build  
          
      - name: SSH Deploy
        uses: appleboy/ssh-action@master
        with:
          EC2_HOST: ${{ secrets.EC2_HOST }}
          EC2_USER: ${{ secrets.EC2_USER }}
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            ssh -o StrictHostKeyChecking=no -i ~/.ssh/id_ed25519 $EC2_USER@$EC2_HOST << 'EOF'
               git clone https://github.com/priyanka-1994/pearlthoughts-first-task-hello-world.git
               cd /home/ubuntu/pearlthoughts-first-task-hello-world
               npm install --production
               npm audit fix
               npm run build
            EOF
              
          
